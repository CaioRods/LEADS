node_modules/
dist/
build/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
.env
.env.local
.env.*.local
AppData/

# skill instalada via npx (33 MB de assets de terceiros)
.agents/
.claude/skills/

# base de leads: contém dados de empresas reais. Versionada por decisão
# explícita do autor em 03/09/2026, com o repositório público.
exemplo-dados.json

# protótipos de design: têm os leads reais embutidos no HTML
design-demos/
assets/logos/*.jpg
assets/logos/*.png
assets/logos/*.webp

# Sessão autenticada do WhatsApp: são as credenciais que dão acesso à conta.
# Publicar isto permitiria a qualquer um sequestrar o WhatsApp. Nunca versionar.
.wwebjs_auth/
.wwebjs_auth.backup/
.wwebjs_cache/

# Configuração local do Claude Code
.claude/

# saída do electron-builder (155 MB por arquivo)
dist/
.cache-atualizacao/
